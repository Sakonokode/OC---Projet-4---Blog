{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body_id 'edit_post' %}

{% block main %}
    {% if is_authenticated() %}
        <div class="row">
            <div class="col-sm-12">
                <div class="well">
                    <form action="{{ '/posts/edit/' ~ post.id }}" method="post">
                        <fieldset>
                            <legend><i class="fa fa-pencil" aria-hidden="true"></i> {{ 'Edit Post' }}</legend>
                            <div class="form-group">
                                <label for="title">{{ 'Title' }}</label>
                                <input type="text" id="title" name="_title" value="{{ post.title }}" class="form-control"/>
                                <label for="description">{{ 'Description' }}</label>
                                <input type="text" id="description" name="_description" value="{{ post.description }}" class="form-control"/>
                                <textarea id="content" name="_content" class="form-control">
                                    {{ post.content.content }}
                                </textarea>
                            </div>
                            <a href="{{ '/posts/' ~ post.id }}" class="btn btn btn-primary">
                                <i class="fa fa-arrow-left" aria-hidden="true"></i> {{ 'Back to Post view' }}
                            </a>
                            <a href="{{ '/posts/delete/' ~ post.id }}" class="btn btn btn-danger pull-right">
                                <i class="fa fa-trash-o" aria-hidden="true"></i> {{ 'Delete' }}
                            </a>
                            <button type="submit" class="btn btn-success pull-right">
                                <i class="fa fa-sign-in" aria-hidden="true"></i> {{ 'Submit Modification' }}
                            </button>
                        </fieldset>
                    </form>
                </div>
            </div>
        </div>
    {% else %}
        <div id="login-users-help" class="panel panel-default">
            <div class="panel-body">
                <p>
                    <span class="label label-danger">{{ 'note' }}</span>
                    {{ 'You are not logged !' }}<br/>
                </p>
            </div>
        </div>
    {% endif %}

{% endblock %}


